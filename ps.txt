The ps command allows you to see which processes the computer is running.
For example ps on it's own will just return the processes running from within the terminal,
however ps x will return ALL the processes running on the computer.
